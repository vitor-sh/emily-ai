# Dependências da Emily
#
# Versões fixadas nas que estão instaladas e testadas na máquina do Vitor.
# O README mandava rodar `pip install -r requirements.txt` desde sempre, mas
# o arquivo não existia.
#
# Projeto é Windows-only: automação, captura de tela e áudio usam pywin32,
# mss, keyboard e ctypes.windll. Não roda em Linux.
#
#   pip install -r requirements.txt
#   playwright install chromium      # navegador.py precisa do browser baixado

# ── LLM ────────────────────────────────────────────────────────────
# openai: cliente de qualquer endpoint OpenAI-compatible (NVIDIA NIM,
#         Groq, DeepSeek, OpenRouter). É o caminho principal do llm.py.
openai==2.32.0
# anthropic: provedor alternativo em llm.py, plano B da migração.
anthropic==0.105.2

# ── Voz ────────────────────────────────────────────────────────────
groq==1.2.0                  # transcrição (Whisper Large V3 Turbo)
fish-audio-sdk==1.3.0        # TTS com voz clonada
PyAudio==0.2.14
SpeechRecognition==3.16.1
sounddevice==0.5.5
pydub==0.25.1                # precisa de ffmpeg no PATH

# ── Visão de tela ──────────────────────────────────────────────────
mss==10.2.0
pillow==12.2.0

# ── Interface ──────────────────────────────────────────────────────
PyQt6==6.11.0
tkinterdnd2==0.4.4.1

# ── Automação do Windows ───────────────────────────────────────────
pywin32==311
keyboard==0.13.5
pynput==1.8.1
pyautogui==0.9.54
pyperclip==1.11.0
psutil==7.2.2
watchdog==6.0.0
py7zr==1.1.0

# ── Pesquisa e navegador ───────────────────────────────────────────
requests==2.34.2
beautifulsoup4==4.15.0
playwright==1.60.0
websockets==16.0

# ── Discord ────────────────────────────────────────────────────────
discord.py==2.7.1
aiohttp==3.13.5

# ── Servidor de controle remoto ────────────────────────────────────
Flask==3.1.3

# ── Notificações de WhatsApp ───────────────────────────────────────
selenium==4.45.0
webdriver-manager==4.1.2

# ── Utilidades ─────────────────────────────────────────────────────
python-dotenv==1.2.2
numpy==2.4.6

# ── Só para gerar o executável (opcional) ──────────────────────────
# pyinstaller==6.21.0
